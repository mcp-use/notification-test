{
  "name": "notification-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "mcp-use dev",
    "build": "mcp-use build",
    "start": "mcp-use start"
  },
  "dependencies": {
    "mcp-use": "latest",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
